status is-interactive || exit

function node_modules_bin -v PWD
    while string match -q "*/node_modules/.bin" -- "$fish_user_paths[1]"
        set -e fish_user_paths[1]
    end
    fish_add_path -g $PWD/node_modules/.bin
end

node_modules_bin
